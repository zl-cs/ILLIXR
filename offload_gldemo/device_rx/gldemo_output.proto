syntax = "proto3";

package gldemo_output_proto;

message texture_handles {
    uint32 left = 1; 
    uint32 right = 2;
}

message swap_indices {
    uint32 front = 1;
    uint32 back = 2;
}

message Rendered_frame {
    texture_handles th = 1;
    swap_indices si = 2;
}